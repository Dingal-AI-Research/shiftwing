# GLM-5.3-Flash: engine qualification and LocalForge wiring

Date: 2026-09-07

## Scope

Establishes that shiftwing can run GLM-5.3-Flash as the deep review lane, and
wires the lane through LocalForge. Covers everything except the real weights,
whose conversion was still running when this was written.

## What is proven

**The engine is correct.** `c/glm53.c` now builds inside shiftwing (`make
glm53`) and is checked against a fixture generated by HuggingFace's own
`Glm5NextTextModel`, so it is measured against an independent implementation
rather than against itself:

| dense precision | max logit delta | tokens |
| --------------- | --------------- | ------ |
| f32             | 9.54e-07        | exact  |
| int8            | 0.0086          | exact  |
| int4            | 0.144           | exact  |

Eight teacher-forced positions and four greedy tokens match at every
precision. These reproduce colibri's own numbers exactly, so the port did not
drift. Guarded by `make -C c test-glm53`.

The int4 row is the reason this model was chosen. Qwen3.8-Flash-Next did not
keep its answers at int4, and never produced coherent output on this machine
at any precision despite six verification cases passing.

**The converted format loads.** The oracle above quantises at load time, which
does not exercise the converter's on-disk layout -- and that layout is exactly
where the Qwen attempt broke (an int4 unpacker reading int8 tables). A tiny
model taken through `convert_glm53.py` and loaded back produces exact tokens,
logits within 0.0378. The path is checked, not assumed.

**The serve path works end to end.** `c/shiftwing serve` with
`SHIFTWING_ENGINE` pointed at `glm53` loads weights, answers `/v1/models`,
applies the chat template, generates, and returns a valid OpenAI completion
with metrics. Verified on the tiny fixture at 32.9 ms TTFT.

## Defects found and fixed

Seven faults, each of which would have stopped or degraded the lane on its own:

1. **No build rule.** `glm53.c` was vendored but never built; conversion would
   have finished with no binary able to read the result.
2. **`st_destroy` did not exist.** The link succeeded only because
   `model_release` is static and unused, so GCC deleted the call. Serving --
   which is what LocalForge needs -- makes that path live and the link fails.
   Implemented in `glm53_compat.h`, releasing every allocation `st_init` makes.
3. **LocalForge dropped every colib lane but Ornith.** `cloneColibSettings`,
   `mergeColibSettings` and `normalizeColibSettings` each named
   `"ornith-397b"` literally, so a second lane vanished between
   `data/settings.json` and the resolved settings, with no error saying why.
   The existing `qwen38fn` entry was already being discarded this way. Now the
   key set is walked, and a regression test covers it.
4. **The engine budget knob differs per engine.** qwen reads `RAM_GB`; glm53
   reads `GLM53_EXPERT_GB`. The launcher passed only the former, so the
   configured `ramGb` was silently ignored. Lanes now carry `engineEnv`.

5. **GLM would have been served Qwen's chat template.** This is the one that
   would have wasted the conversion. `openai_server.py` contains a correct GLM
   renderer, but a later function of the same name shadowed it -- deliberately,
   with a comment calling it dead, from when the deep lane moved to Ornith. So
   every non-Ornith model was rendered as Qwen. GLM-5.3's template is
   `[gMASK]<sop>` with `<|system|>`/`<|user|>`/`<|assistant|>` and contains no
   `<|im_start|>` anywhere: the model would have been prompted entirely in
   control tokens it was never trained on, and the result would have looked
   like a bad model rather than a wiring fault -- exactly how the Qwen attempt
   presented. The renderer is now reached by name, dispatched on the family
   marker, and `convert_glm53.py` stamps `shiftwing_model_family: "glm-5.3"`
   into the converted config (the existing partial conversion was stamped in
   place). Tool-call parsing had the same shadowed pair and is dispatched the
   same way, though the review gate is tool-free.

   Verified: `render_model_chat(None, "glm-5.3", ...)` yields
   `[gMASK]<sop><|system|>...<|user|>...<|assistant|><think></think>`, and all
   seven GLM control tokens resolve to single ids in the real tokenizer.

6. **The tokenizer split multi-digit numbers wrongly.** Found by running the
   existing 10,000-case parity gate against GLM's tokenizer, which had never
   been done. The pre-tokenizer regex differs in exactly one place:

       ornith/qwen : \p{N}        one digit per piece
       glm53       : \p{N}{1,3}   up to three digits per piece

   `tok.h` had Qwen's rule hardcoded, so `"19"` encoded as `"1"+"9"` instead of
   the single token GLM was trained on. **6412 of 10000 cases diverged from the
   HuggingFace oracle.** Every number in a diff -- line numbers, limits,
   offsets, versions -- would have reached the model tokenized differently from
   what it expects, which is a quiet accuracy tax on exactly the material a
   code reviewer reads. Decode round-tripped perfectly throughout, so nothing
   downstream would have looked wrong.

   The rule is now read from the tokenizer instead of assumed. GLM goes to
   **10000/10000**, and Ornith stays at 10000/10000 -- `tok.h` is shared with
   the lane currently in production, so that regression check was the gate on
   the change. No tokenizer in the repo exercised the multi-digit rule, which
   is why nothing caught it; `tests/test_tok_digit_run.c` now holds both rules.

GLM's default here is safer than Qwen's: it measures `MemAvailable` and leaves
a 3 GB margin, rather than defaulting to loading every expert.

7. **The doctor preflight could not read a GLM config.** LocalForge runs
   `shiftwing doctor` as a hard gate before serving, and the resource planner
   raised `KeyError('num_experts')` on GLM, whose config calls that field
   `n_routed_experts`. The lane could not start at all, with perfect weights.
   Renaming would not have sufficed: GLM stores KV as an MLA latent
   (`kv_lora_rank` per token per full layer) where Qwen stores
   `2 * kv_heads * head_dim`, so Qwen's formula overstates KV by more than an
   order of magnitude and would refuse a host that fits comfortably. GLM now
   has its own planner, dispatched on the family. Container size is measured
   from the converted snapshot rather than predicted -- the file sizes are the
   ground truth the disk check is about -- while the routed-expert share is
   computed analytically, which is exact and separates streamed weights from
   the resident remainder.

An eighth, found while testing the teardown of the validation job: killing the
launcher does not stop the server -- the HTTP process survives holding the port
and the engine. With the real checkpoint that orphan would pin ~190 GB. The job
now runs the server in its own process group via `set -m` and signals the
group. `setsid` was tried first and is wrong here: `$!` becomes the wrapper,
which exits immediately, so the teardown signalled an empty group and left the
port held.

`SHIFTWING_ENGINE`/`COLI_ENGINE` and the `GLM53_*` knobs are now scrubbed from
the inherited environment, so a value in the launching shell cannot serve one
architecture's weights with another's engine.

Context had the same shape of problem as the memory budget: the launcher sets
`CTX`, which glm53 does not read. Its KV depth comes from `GLM53_MAXT` and
otherwise defaults to 8192, so a lane asking for 16k would have quietly run at
half of it and truncated the diff under review -- silently, and only on long
inputs. The launcher now sets both names from `--context`, and both budget
names from `--ram-gb`, keeping one source of truth. Confirmed by reading the
engine's own `/proc/<pid>/environ`.

## The lane starts under LocalForge

Proven with the real coordinator, not by inspection:

    coordinator.start("auditor397b")
      -> resolves the glm53-flash lane from auditAgentModel
      -> doctor preflight passes
      -> launches with SHIFTWING_ENGINE=c/glm53
      -> lifecycle "ready"
      -> GET /v1/models  200  {"id":"glm53-flash"}
      -> shutdown        lifecycle "stopped", no orphan

Run against the tiny fixture, so it exercises the coordinator rather than the
weights. Every layer between LocalForge and the engine is now covered by
something executed rather than reasoned about.

The doctor also reports the number that matters most for the review lane:
**0.49 GiB of runtime state for one slot at 16384 context**. MLA's latent KV is
what buys that, and it is why a longer review context is affordable here in a
way it was not on the Ornith lane.

## Lane configuration

`data/settings.json` gains a `glm53-flash` colib lane (engine `c/glm53`,
weights `c/glm53_i4`, `GLM53_EXPERT_GB=12`, 16384 context) and a matching
deterministic model profile. Selecting it is a one-line change to
`auditAgentModel`; the lane already resolves by that field.

Not yet activated: `auditAgentModel` still reads `ornith-397b`, because the
weights do not exist yet.

## Ornith measurement, for comparison

The Ornith lane was proven end to end before this work: valid JSON, correct
`changes_required` verdict, and both required actions naming the planted
defect -- in **8350 s (139 minutes)** for a single 16K review. Correct, and
too slow to use. That measurement is what makes GLM worth the conversion.

## What remains

Conversion of the 62 shards, download-bound at roughly 7-8 minutes per shard
(unauthenticated HF throttling, ~5 MB/s; no token is configured on this
machine). Job `210-glm53-validate.sh` is queued behind it and runs
automatically: it refuses to measure an incomplete model, serves on port 8125
so a running LocalForge is untouched, smoke-tests generation, then scores one
review case over HTTP.

Scoring goes over HTTP because glm53 has no chat mode in its CLI -- the
template lives in `openai_server.py` -- and because HTTP is the path
LocalForge actually uses. `review_quality_bench.py` gained an `--endpoint`
backend for this, which also makes the two engines comparable on one code
path.

## Result on the real checkpoint

Conversion produced 62 shards, 181.34 GiB. The model was correct on its first
real request -- it answered `ready` and stopped on a real stop token -- but the
review task did not complete at all until the read path was fixed.

| run | expert reads | review of one planted defect |
| --- | --- | --- |
| Ornith-397B | -- | correct, **139 min** |
| GLM, buffered | `st_pread_full` | **never finished**, timed out at 120 min |
| GLM, O_DIRECT | `st_pread_direct_raw` | correct, **14.3 min** |

**9.7x faster than the lane it replaces**, on the same task, with a verdict that
cites the work specification rather than merely being well-formed:

> Restore the previous behaviour of throwing NotFoundError(id) when user is not
> found, or provide explicit approval that the behavioural change is intended;
> the spec requires preserving existing behaviour.

> Add tests covering both the found-user path (returning user.profile) and the
> not-found path to satisfy the spec's test coverage requirement.

`json_ok 1, verdict_ok 1, caught 1` at 155.7 s to first token and 192 output
tokens.

## The read path was the whole story

Two configuration faults were found first, and neither was the real cause.

The expert cache was set to 12 GiB on top of 21.9 GiB of resident weights, on a
VM capped at 30 GiB -- an over-commit introduced in this work by mapping the
lane's `ramGb` onto `GLM53_EXPERT_GB` without checking GLM's resident
footprint, which is far larger than Ornith's. The machine evicted continuously.
The resource planner added here reports that configuration as `ram: FAIL`, and
that is how it was found. Correcting it to 4 GiB bought 2.3x and was not
nearly enough.

What remained was measured directly, on the model's own shards, with the engine
resident and the page cache full:

    O_DIRECT sequential   408 MB/s
    buffered  sequential  15.9 MB/s

`expert_read` used the buffered path. It is the hottest read in the engine --
roughly 4.5 GB per generated token, since every token wants 8 experts on each
of 42 sparse layers -- and it was competing for page cache with the very model
it was reading. The engine was moving 30-66 MiB/s while the host sat 77-94%
idle: not compute-bound, not thrashing any more, simply reading through the
wrong door.

Routing expert reads through the O_DIRECT twin descriptor `st.h` already opens,
with the buffered read kept as the fallback a filesystem without O_DIRECT
needs:

| | buffered | O_DIRECT | |
| --- | --- | --- | --- |
| cold TTFT (13 tokens) | 189.8 s | 121.9 s | 1.56x |
| warm TTFT (13 tokens) | 81.0 s | 33.1 s | 2.45x |
| sustained expert read | 30-66 MiB/s | 314 MB/s cold, 1.15 GB/s warm | ~20x |

The tiny oracle still passes at f32 (9.54e-07) and int4 (0.144) with the new
binary, so this changed throughput and nothing else. `GLM53_DIRECT=0` restores
the old path for comparison.

Worth recording: the warm pass still reads 38 GB for a 13-token prompt. A 4 GiB
cache holds nothing against 159.5 GiB of routed experts, so the engine re-reads
almost everything. It is now simply fast enough that this does not decide the
outcome. A larger cache is not available on this host; more RAM would be the
next lever, not more tuning.

## Lane activated

`auditAgentModel` is now `glm53-flash`. The lane resolves to the GLM engine and
the converted weights, at 16384 context with a 4 GiB expert budget, and the
configured audit timeout of 180 minutes has ample margin over the measured 14.3.

## Open risk

One review case has been scored, not the full set of five. The verdict is
correct and the timing is sound, but "reviews as well as Ornith" is a claim
about quality across cases and this is one data point.

Throughput depends on a disk that is quick under O_DIRECT and very slow without
it. Anything that silently returns the engine to buffered reads -- a filesystem
that refuses O_DIRECT, a copy of the model onto other storage -- costs an order
of magnitude, and the fallback is deliberately quiet because a filesystem
without O_DIRECT is a normal condition rather than an error.
